<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD</title>
</head>
<body>
    <h1>This is CRUD</h1>

    <form action="/quotes" method="POST">
        <input type="text" name="name" id="name">
        <input type="text" name="quote" id="quote">
        <button type="submit">Submit</button>
    </form>

    <div>
        <h2>Luigi invades!</h2>
        <p>
          Replace first Yoshi's quote with a quote written by Luigi
        </p>
        <button id="update-button">Replace Yoshi's quote</button>
      </div>

      <div>
        <h2>Remove Luigi!</h2>
        <p>
          Delete one Luigi's quote. Does nothing if there are no more Luigi quotes
        </p>
        <button id="delete-button">Delete Luigi's quote</button>
      </div>

      <div id="message"></div>
    
    <h2> Quotes </h2>

    <ul class="quotes">
    <!-- Loop through quotes -->
    <% for(var i = 0; i < quotes.length; i++) {%>
    <li class="quote">
      <!-- Output name from the iterated quote object -->
      <span><%= quotes[i].name %></span>:
      <!-- Output quote from the iterated quote object -->
      <span><%= quotes[i].quote %></span>
    </li>
    <% } %>
    </ul>

    <script src="/main.js"></script>
</body>
</html>